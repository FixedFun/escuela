extends index

block content
  h1 #{pagina}

  if id
    p Ingrese el código: #{id}
  
    form(method="post", action=`/buscaEstudiante/${id}`)
      button.btn(type="submit") Buscar Alumno

  else
    p Se necesita un código.
    
  form(method="get", action="/buscaEstudiante")
    label(for="id") Ingrese el código:
    input(type="text", name="id", placeholder="Código")
    button.btn(type="submit") Buscar
